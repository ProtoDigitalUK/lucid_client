/**
 * Refreshes the library
 * - Re-creates attribute maps
 * - Re-initialises stores
 * - Re-creates the observer
 * - Re=initialises handlers
 */
const refresh = () => {};

export default refresh;
