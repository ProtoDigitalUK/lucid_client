import initialiseHandlers from "./initialise-handlers.js";
import createAttributeMap from "./create-attribute-map.js";

const handler = {
	initialiseHandlers,
	createAttributeMap,
};

export default handler;
